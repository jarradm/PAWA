JSON2.js is to support IE7, IE8 with some Doctypes might need it as well

in Site.master a js function is assigned to awe.err, this will show popup in the left bottom corner of the screen any time an ajax request to the server will ecounter an error

changing themes is being handled by the ChangeThemeController and a bit of js in Site.js

Menu on the left is an Awesome Grid helper you can see it Site.master